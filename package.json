{
  "name": "geo-grouping",
  "version": "1.0.0",
  "description": "Cluster geolocation points into groups of 3 and classify by distance tier",
  "type": "module",
  "scripts": {
    "start": "npx tsx src/index.ts",
    "build": "tsc"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
